import { BlogPage } from "@/app/pages/BlogPage";

export const metadata = {
  title: 'Journal',
  description: 'Read stories, recipes, and insights from the Savora kitchen and cellar.',
};

export default function Page() {
  return <BlogPage />;
}
