import { MenuPage } from '@/app/pages/MenuPage';

export const metadata = {
  title: 'Menu',
  description: 'Explore Savora’s signature dishes, seasonal tasting menu, and curated beverage list.',
};

export default function Page() {
  return <MenuPage />;
}
