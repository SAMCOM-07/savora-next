import { GalleryPage } from '@/app/pages/GalleryPage';

export const metadata = {
  title: 'Gallery',
  description: 'View the atmosphere, design, and culinary artistry behind the Savora experience.',
};

export default function Page() {
  return <GalleryPage />;
}
