import { About } from "@/components/About"

export const metadata = {
  title: 'About',
  description: 'Discover the story behind Savora, our culinary philosophy, and the team that brings our vision to life.',
};


const page = () => {
  return (
    <About />
  )
}

export default page
